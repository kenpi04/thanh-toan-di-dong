@model BannerModel
@{
    //page title
    ViewBag.Title = @T("Admin.Configuration.Banner.AddNew").Text;
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "banner-form" }))
{
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Administration/Content/images/ico-catalog.png")" alt="" />
            @T("Admin.Configuration.Banner.AddNew") @Html.ActionLink("(" + T("Admin.Configuration.Banner.BackToList") + ")", "List")
        </div>
        <div class="options">
            <input type="submit" name="save" class="t-button" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="t-button" value="@T("Admin.Common.SaveContinue")" />
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}