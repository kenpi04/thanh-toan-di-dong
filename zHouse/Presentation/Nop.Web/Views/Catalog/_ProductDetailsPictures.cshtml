@model ProductDetailsModel
@using Nop.Core.Domain.Media
@using Nop.Core.Infrastructure
@using Nop.Web.Models.Catalog;
@{
    Html.AddScriptParts("~/Scripts/slimbox2.js");

    
}
<div class="gallery">
    <div class="picture" id="main-pic">
        @if (Model.DefaultPictureZoomEnabled)
        {
            <a href="@Model.DefaultPictureModel.FullSizeImageUrl" data-gallery="lightbox-pd" title="@Model.Name"  id="main-product-img-lightbox-anchor-@Model.Id">
                <img   alt="@Model.DefaultPictureModel.AlternateText" src="@Model.DefaultPictureModel.FullSizeImageUrl" title="@Model.DefaultPictureModel.Title" itemprop="image" id="main-product-img-@Model.Id" />
            </a>
        }
        else
        {
            <img  alt="@Model.DefaultPictureModel.AlternateText" src="@Model.DefaultPictureModel.FullSizeImageUrl" title="@Model.DefaultPictureModel.Title" itemprop="image" id="main-product-img-@Model.Id" />
        }
        <div class="picture-title" id="pic-title"></div>

    </div>
    @if (Model.PictureModels.Count > 1)
    {
        <div class="thumbnail">
            @for (int i = 0; i < Model.PictureModels.Count; i++)
            {
                var picture = Model.PictureModels[i];

               <div class="thumbnailPic">
                    <a href="@picture.FullSizeImageUrl" rel="@picture.Description" data-gallery="lightbox-p" title="@Model.Name">
                        <img  src="@picture.ImageUrl" alt="@picture.AlternateText" class="noborder" title="@picture.Title" />
                </a>     
               </div>
            }
        </div>
        <script type="text/javascript">
            $(document).ready(function () {
                $(".thumbnailPic a").click(function () {
                    var src=$(this).attr("href");
                    $("#main-pic img").attr("src", src);
                    $("#pic-title").html($(this).attr("rel"))
                }).mouseover(function () {
                    var src = $(this).attr("href");
                    $("#main-pic img").attr("src", src);
                    $("#pic-title").html($(this).attr("rel"))
                })
            })
        </script>
    }
</div>
