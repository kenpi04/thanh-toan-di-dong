@model List<PlanX.Web.Models.News.NewsItemModel>
@if (Model.Count > 0) {
    <ul class="list_related">
        @foreach (var item in Model)
        {<li><a href="@Url.RouteUrl("NewsItem", new{SeName = @item.SeName})" title="@item.Title"><small>>></small>@item.Title</a></li>}    
</ul>}